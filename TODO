Setup raider.io API polling
    Filter through list of characters and realms
    If a character/realm matches, then update the keystone with a higher key 

Complete character tracking
    if a user adds a character to the tracker, insert it into the usercharacters table 
    Two days before the weekly reset, filter through the list, and notify users (by their UID)
     if a character hasn't done a keystone yet.
    After reset, erase the keystones
    
Finish up any unfinished sections in wow.py, app.py, etc.

Polish the HTML templates
    Clean up wording
    Fix JavaScript if needed
    Minify, minify, minify!

Finish this list!!! <<<---- IMPORTANT